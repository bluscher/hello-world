version: 2.1
jobs:
  print_hello:
    docker:
      - image: circleci/node:13.8.0
      #- image: cimg/base:stable >> original 
    steps:
      #- checkout >> original config
      - run:
          #name: "print_hello"
          command: "echo Hello, job 1 !"
  print_world:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run:
          #name: "print_world"
          command: "echo World, job 2 !"
workflows:
  say-hello_world-workflow:
    jobs:
      - print_hello
      - print_world:
          requires:
            - print_hello

